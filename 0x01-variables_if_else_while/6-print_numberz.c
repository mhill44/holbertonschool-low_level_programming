#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/**
 * ALWAYS RETURN 0 step 6 of Print the alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int c;

    for (c = 48; c <= 57 ; c++)
        putchar(c);
    putchar('\n');
    return (0);
}
