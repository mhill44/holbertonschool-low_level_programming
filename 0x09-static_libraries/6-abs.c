#include "holberton.h"
#include <math.h>
#include <stdlib.h>
#include <stdio.h>
/**
* _abs - computes the absolute value of an integer
* for the return: a for absolute integer
*/
int _abs(int a)
{
if (a < 0)
return (a = -a);
else
return (a);
}
