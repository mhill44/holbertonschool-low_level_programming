#include "holberton.h"
#include <math.h>
#include <stdlib.h>
#include <stdio.h>
/**
* _islower checks for lowercase letter
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
return (0);
}
